from setuptools import setup

setup(
    name='personas',
    version='1.0.0',
    description='Python client for the Segment Profile API.',
    packages=['profile'],
    install_requires=['requests']
)

